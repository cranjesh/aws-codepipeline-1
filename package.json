{
  "name": "monitor-test-lambda",
  "version": "1.0.1",
  "dependencies": {
    "aws-sdk": "^2.1598.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "monitor-test": "echo \"Error: monitor test\" && exit 0"
  }
}
